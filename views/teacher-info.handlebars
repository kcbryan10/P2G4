<h2>{{teacherName}}</h2>
<span>{{teacherSpecialties}}</span>

<p>{{teacherBio}}</p>

<div class='available-timeslots-wrapper'>
  <div class='day'>
    <p>Sunday</p>
    <ul class='available-timeslots'>
      {{#each timeslots as |timeslot|}}
      {{#if (checkDay 1 timeslot.weekly_timeslot.day)}}
      <li>
        {{> available-timeslots timeslot}}
      </li>
      {{/if}}
      {{/each}}
    </ul>
  </div>
  <div class='day'>
    <p>Monday</p>
    <ul class='available-timeslots'>
      {{#each timeslots as |timeslot|}}
      {{#if (checkDay 2 timeslot.weekly_timeslot.day)}}
      <li>
        {{> available-timeslots timeslot}}
      </li>
      {{/if}}
      {{/each}}
    </ul>
  </div>
  <div class='day'>
    <p>Tuesday</p>
    <ul class='available-timeslots'>
      {{#each timeslots as |timeslot|}}
      {{#if (checkDay 3 timeslot.weekly_timeslot.day)}}
      <li>
        {{> available-timeslots timeslot}}
      </li>
      {{/if}}
      {{/each}}
    </ul>
  </div>
  <div class='day'>
    <p>Wednesday</p>
    <ul class='available-timeslots'>
      {{#each timeslots as |timeslot|}}
      {{#if (checkDay 4 timeslot.weekly_timeslot.day)}}
      <li>
        {{> available-timeslots timeslot}}
      </li>
      {{/if}}
      {{/each}}
    </ul>
  </div>
  <div class='day'>
    <p>Thursday</p>
    <ul class='available-timeslots'>
      {{#each timeslots as |timeslot|}}
      {{#if (checkDay 5 timeslot.weekly_timeslot.day)}}
      <li>
        {{> available-timeslots timeslot}}
      </li>
      {{/if}}
      {{/each}}
    </ul>
  </div>
  <div class='day'>
    <p>Friday</p>
    <ul class='available-timeslots'>
      {{#each timeslots as |timeslot|}}
      {{#if (checkDay 6 timeslot.weekly_timeslot.day)}}
      <li>
        {{> available-timeslots timeslot}}
      </li>
      {{/if}}
      {{/each}}
    </ul>
  </div>
  <div class='day'>
    <p>Saturday</p>
    <ul class='available-timeslots'>
      {{#each lessons as |timeslot|}}
      {{#if (checkDay 7 timeslot.weekly_timeslot.day)}}
      <li>
        {{> available-timeslots timeslot}}
      </li>
      {{/if}}
      {{/each}}
    </ul>
  </div>
</div>

<script src="/js/schedule-timeslot.js"></script>